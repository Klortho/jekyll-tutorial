# Site settings
title: Gorbulon the Magnificent
email: voldrani@gmail.com
description: > # this means to ignore newlines until "baseurl:"
  This is a test site, that I'm using to learn Jekyll.
baseurl: "" # the subpath of your site, e.g. /blog/
url: "http://chrismaloney.org" # the base hostname & protocol for your site
twitter_username: klortho
github_username:  klortho

# Build settings
markdown: kramdown

# Default values for every page (override these in the page's YAML front matter)
defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "posts"  # possible types are pages, posts, drafts, or any collection
    values:
      layout: "post"

